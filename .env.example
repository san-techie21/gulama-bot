# Gulama Bot — Environment Variables
# Copy this file to .env and fill in your values.
# IMPORTANT: Never commit .env to version control.
# Credentials should be stored in the encrypted vault instead.
# These env vars are ONLY for initial setup / CI environments.

# LLM Provider API Key (use vault for production)
# Supports: Anthropic, OpenAI, DeepSeek, Qwen, Zhipu, Moonshot, Groq, etc.
# LLM_API_KEY=your-api-key-here

# Telegram Bot Token (use vault for production)
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Override data directory (optional)
# GULAMA_DATA_DIR=~/.gulama

# Override config file path (optional)
# GULAMA_CONFIG_FILE=~/.gulama/config.toml

# Log level override (optional)
# GULAMA_LOG_LEVEL=DEBUG
